#include<iostream>
using namespace std;

int main() {
	
	//Event Odd Number using AND operation
	/*
	int n;
	cin>>n;
	if((n & 1) == 0){
		cout << "The number is Even" << endl;
	} else{
		cout << "The number is Odd" << endl;
	}
	*/
	
	//Swap 2 number using XOR operation
	/*
	int a, b;
	cin >> a >> b;
	cout << "The numbers before swap are " << a << " " << b << endl; 
	a = a ^ b;
	b = a ^ b;
	a = a ^ b;
	cout << "The numbers after swap are " << a << " " << b << endl; 
	*/
	
}
